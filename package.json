{
  "name": "big-walk-guide",
  "version": "1.0.0",
  "private": true,
  "description": "Big Walk unofficial fan guide — pre-rendered static site. Content source of truth is i18n/*.js; build.js bakes the default (English) text into the static HTML so crawlers and AdSense can read it without JavaScript.",
  "scripts": {
    "build": "node build.js"
  },
  "dependencies": {
    "jsdom": "^30.0.1"
  }
}
